package chapter6;

/**
 * 题目:求1 + 2 + ... + m,要求不能使用乘除法、for、 while if else、 switch case等关键字及条件判断语句(A?B:C)。
 *
 * @author flying
 */
public class Q64_Accumulator {
    /**
     * 从 1 + ... + n 不能使用乘法和除法（不能用数学推断），不能循环，
     * 不能使用 if else ，就是只能顺序执行，不能有分支和判断。。。
     *
     * 看了答案，需要利用 java  java 干不出来。。。
     */
    private int accumultor(int n) {
        return -1;
    }
}